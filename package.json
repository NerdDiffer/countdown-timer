{
  "name": "countdown-timer",
  "version": "0.0.0",
  "description": "Create the HTML/CSS/JS for a simple countdown timer",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prebuild": "mkdir -p public/build",
    "build": "browserify src/clock.js -o public/build/bundle.js"
  },
  "keywords": [],
  "author": "Rafael Espinoza <rafael@rafaelespinoza.com> (www.rafaelespinoza.com)",
  "license": "ISC",
  "dependencies": {
    "timepiece": "^0.4.0"
  },
  "devDependencies": {
    "browserify": "^14.0.0"
  }
}
